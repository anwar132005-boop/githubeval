pipeline {
    agent any

 

    stages {
        stage('Checkout SCM') {
            steps {
                git branch: 'main', url: 'https://github.com/anwar132005-boop/githubeval.git'
            }
        }

 

        stage('Install Docker') {
            steps {
                sh '''
                chmod +x install_docker.sh
                sudo ./install_docker.sh
                '''
            }
        }
    }
}
